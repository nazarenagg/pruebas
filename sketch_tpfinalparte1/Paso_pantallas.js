function cambioPantallas (){

 if ( estado === 1 ) {
    mostrar( 1 , dialogos , [["   ", 220 , 370 , 200 , 50] , ["   ", 270 , 430 , 100 , 40]] );
}
  else if ( estado === 2 ) {
    mostrar( 2 , dialogos , [["Regresar", 530, 10 , 110 , 30]] );
} 
  else if ( estado === 3 ) {
    mostrar( 3 , dialogos , [["camara", 80, 400] , ["pasillo", 350, 400]] );
} 
  else if ( estado === 4 ) {
    mostrar( 4 , dialogos , [["seguir", 350, 400]] );
} 
  else if ( estado === 5 ) {
    mostrar( 5 , dialogos , [["seguir", 350, 400]] );
} 
  else if ( estado === 6 ) {
    mostrar( 6 , dialogos , [["volver", 80, 400] , ["seguir", 350, 400]] );
} 
  else if ( estado === 7 ) {
    mostrar( 7 , dialogos , [["seguir", 350, 400]] );
} 
  else if ( estado === 8 ) {
    mostrar( 8 , dialogos , [["seguir", 350, 400]] );
} 
  else if ( estado === 9 ) {
    mostrar( 9 , dialogos , [["izquierda", 80, 400] , ["derecha", 350, 400]] );
} 
  else if ( estado === 10 ) {
    mostrar( 10 , dialogos , [["volver", 350, 400]] );
} 
  else if ( estado === 11 ) {
    mostrar( 11 , dialogos , [["volver", 350, 400]] );
} 
  else if ( estado === 12 ) {
    mostrar( 12 , dialogos , [["cerrar la puerta", 80, 400] , ["esconderse", 350, 400]] );
} 
  else if ( estado === 13 ) {
    mostrar( 13 , dialogos , [["srguir", 350, 400]] );
} 
  else if ( estado === 14 ) {
    mostrar( 14 , dialogos , [["seguir", 350, 400]] );
} 
  else if ( estado === 15 ) {
    mostrar( 15 , dialogos , [["volver", 350, 400]] );
} 
  else if ( estado === 16 ) {
    mostrar( 16 , dialogos , [["izquierda", 80, 400] , ["derecha", 350, 400]] );
} 
  else if ( estado === 17 ) {
    mostrar( 17 , dialogos , [["volver", 350, 400]] );
} 
  else if ( estado === 18 ) {
    mostrar( 18 , dialogos , [["seguir", 350, 400]] );
} 
  else if ( estado === 19 ) {
    mostrar( 19 , dialogos , [["seguir", 350, 400]] );
} 
  else if ( estado === 20 ) {
    mostrar( 20 , dialogos , [["otra salida", 80, 400] , ["prender fuego", 350, 400]] );
} 
  else if ( estado === 21 ) {
    mostrar( 21 , dialogos , [["seguir", 350, 400]] );
} 
  else if ( estado === 22 ) {
    mostrar( 22, dialogos , [["seguir", 350, 400]] );
} 

}//--------------------------------------------------------------------------corchete de cierre del CAMBIO DE PANTALLA
